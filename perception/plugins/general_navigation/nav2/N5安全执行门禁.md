# N5 Driver 安全执行合同

本文档记录 `general_navigation` 与现有 G1 Driver `loco` actuator 之间的实际联调
合同。Driver 实现来自 [He2y/phanthymotus-driver](https://github.com/He2y/phanthymotus-driver/tree/main)；
Perception 不复制 Driver 代码，不调用 Unitree SDK。

## 责任边界

| 能力 | owner |
|---|---|
| 14-action、地图、定位、Nav2 goal/path/readiness | Perception `general_navigation` |
| `velocity_proposal.v1` 产生与终态零速 | Nav2 companion |
| 任务 `nav_id` 生成、精确画布连线和 Driver lifecycle | Agent Core |
| proposal 校验、主运控、TTL/watchdog、Move/StopMove、停车确认 | Driver `loco` |

## 画布合同

```text
Driver.loco_state ──> general_navigation.loco_state
Driver.lidar_cloud ──> general_navigation.lidar
general_navigation.velocity_proposal ──> Driver.loco.velocity_proposal
```

必须恰好存在这三条连线。执行连线的两端必须同时匹配 port、topic、format、
ROS type 和 schema。

## proposal 合同

Driver `loco.topic_in` 声明：

```json
{
  "port": "velocity_proposal",
  "topic": "/ubuntu/navigation/nav2/velocity_proposal",
  "format": "data/json",
  "ros_type": "std_msgs/msg/String",
  "qos": "RELIABLE + KEEP_LAST(depth=10) + VOLATILE",
  "schema": "phanthy.navigation.velocity_proposal.v1"
}
```

权威 schema 和示例：

- [`velocity-proposal-v1.schema.json`](protocol/velocity-proposal-v1.schema.json)
- [`velocity-proposal-v1.example.json`](protocol/velocity-proposal-v1.example.json)

Driver 上限：

| 字段 | 上限 |
|---|---:|
| `ttl_ms` | 250 ms |
| `velocity.x` | `[-0.05, 0.15] m/s` |
| `abs(velocity.y)` | `0.12 m/s` |
| `abs(velocity.yaw)` | `0.35 rad/s` |
| `hypot(x,y)` | `0.18 m/s` |

`paused/arrived/cancelled/stopped/error/aborted/rejected` 必须是零速。Driver 另外拒绝错
schema、错 frame、非有限数、伪造执行 flag、超速、过期 TTL、重放 sequence 和非当前
`nav_id`。

## 可信 lifecycle

`nav_id` 不由 LLM 或 Perception 生成。Agent Core 在画布连线校验后生成 ID，调用：

```json
{
  "action": "start",
  "instance_id": "<loco canvas card id>",
  "input_topic": "/ubuntu/navigation/nav2/velocity_proposal",
  "expected_nav_id": "<Agent Core generated id>"
}
```

Driver 必须返回 `state=ready, connected=true, armed=true, expected_nav_id=<same id>`，
Agent Core 才会把同一 ID 通过私有 `_control_nav_id` 交给 `general_navigation`。该字段
不出现在公开 action schema 中。

到达、显式停止、暂停、错误或 timeout 后，Agent Core 调用：

```json
{"action":"stop","instance_id":"<loco canvas card id>"}
```

只有 Driver 返回 `state=idle, connected=false, stop_confirmed=true` 才释放 lease。否则导航
返回 `execution_driver_stop_unconfirmed`，项目停止也保持运行态供人工处置。

Driver 会 retire 终止的 `nav_id`，所以 pause 后不允许用旧 lease resume；必须发起
新导航任务。

## 项目启停

- 项目 start 首先校验执行连线；失败时不启动任何卡片。
- 传感器和 `general_navigation` 启动后，`loco.info` 必须显示
  `enabled=true, connected=false, armed=false`，Driver 保持 standby。
- 项目 stop 先停 Driver，再停其他卡片。Driver 停车未确认时项目不标记为已停止。
- Driver 重启后默认未连接、未武装。

## 输入现状

Driver 当前输出 legacy loco JSON 和 `<II>` pointcloud envelope，不带源时间戳/frame。
Nav2 adapter 以 ROS 接收时间和显式 `livox_frame` 合同兼容，并在状态中标记元数据
来源。这是当前输入质量的真实边界，不是 Driver 授权回执。

## 验收门禁

只读：

```bash
./scripts/loco-integration-readiness.sh
STAGE=preflight ./scripts/owner-loco-card-acceptance.sh
```

有人值守物理验收：

```bash
I_AM_G1_OWNER=1 I_HAVE_G1_REMOTE=1 \
  STAGE=move MAP_NAME=g1-n3-acceptance \
  ./scripts/owner-loco-card-acceptance.sh
```

通过必须同时满足：

1. Agent Core 项目正在运行，画布三条连线精确；
2. 真实 odom/scan/map/TF 新鲜；
3. 命令经 Agent Core 调用 `general_navigation`，不直连 Perception/Driver；
4. Driver 和 proposal 使用同一 `nav_id`，sequence 递增且 TTL=250 ms；
5. 机器人实测移动至少 0.08 m，Nav2 返回 `arrived`，终点误差不超过 0.30 m；
6. 终态 proposal 零速，Driver stop 已确认并回到 standby。

任一门禁失败都执行 fail closed，不得将“Driver RPC 返回 0”、“有 subscriber”或
“机器人看起来动了”单独当作完整卡片验收。
