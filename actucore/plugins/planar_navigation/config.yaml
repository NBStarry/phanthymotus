mcp_port: 15730
plugins:
  planar_navigation:
    enabled: true
    namespace: planar
    scan_topic: /scan
    odom_topic: /odom
    rgb_topic: ""
    driver_status_topic: ""
    data_dir: /data/planar_navigation
    # Configure measured footprint before localization; [] deliberately blocks it.
    footprint: []
    execution_enabled: false
